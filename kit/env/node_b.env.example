# node_b.env — the REMOTE node (answers pings, reports its measured SNR).
# Same structure as node_a.env; only the values differ.

MODEM_HOST=127.0.0.1
MODEM_PORT=5055
MODEM_TOKEN=your-modem-token-or-empty
API_BIND=10.8.0.11
API_PORT=8080
API_KEY=choose-a-long-random-key
LABEL=node_b
URL=http://10.8.0.11:8080